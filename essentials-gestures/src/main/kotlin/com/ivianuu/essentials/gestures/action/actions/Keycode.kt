package com.ivianuu.essentials.gestures.action.actions

// todo

/**
import com.ivianuu.pie.R
import com.ivianuu.pie.action.Action
import com.ivianuu.pie.data.Flag

private fun createKeycodeAction() = Action(
    key = KEY_KEYCODE,
    title = string(R.string.action_keycode),
    states = stateless(R.drawable.ic_keyboard),
    flags = setOf(Flag.RequiresRoot)
)

private suspend fun doKeycode(key: String) {
    val keycode = key.split("=:=")[1]
    runRootCommand("input keyevent $keycode")
}

*/
